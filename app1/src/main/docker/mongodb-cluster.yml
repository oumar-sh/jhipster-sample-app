version: '2'
services:
  react-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/react-mongodb-config --bind_ip 0.0.0.0
  react-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  react-mongodb-config:
    image: mongo:4.2.7
    container_name: react-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
